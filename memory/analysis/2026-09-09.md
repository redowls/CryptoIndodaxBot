# CryptoIndodaxBot — Analysis 2026-09-09 (UTC)

Snapshots today: 17. Watchlist: 10 coins. Engine regime: **risk_on**
(`computed risk_on, policy hint auto -> risk_on`, every cycle). 0 trades closed,
0 opened. Two positions carried, both underwater.

## Board (structure the engine actually reads)

The digest prints 1H only; the regime gate reads BTC 1D. Full stack:

| sym | 1H | 4H | 1D | 1D ADX | 1D RSI | 1H ADX | 1H RSI | ATR% | fee drag | day % |
|-----|----|----|----|--------|--------|--------|--------|------|----------|-------|
| BTC | **DOWN** | DOWN | UP | 50.9 | 59.9 | 24.1 | 44.9 | 0.48 | **44%** | −0.13 |
| ETH | MIX | UP | UP | 52.0 | 62.8 | 9.8 | 48.1 | 0.57 | **37%** | −0.24 |
| SOL | **DOWN** | MIX | UP | 45.5 | 61.4 | 15.1 | 45.1 | 0.76 | 28% | −0.69 |
| XRP | MIX | UP | UP | 43.4 | 60.2 | 18.1 | 47.7 | 0.79 | 27% | −0.43 |
| DOGE | MIX | UP | UP | 42.8 | 59.6 | 12.0 | 41.5 | 0.86 | 24% | −1.76 |
| AVAX | **DOWN** | UP | UP | 39.0 | 62.4 | 18.0 | 41.5 | 0.65 | **32%** | −1.96 |
| LINK | **DOWN** | MIX | UP | **61.4** | 58.2 | 36.6 | 34.3 | 1.15 | 18% | −4.49 |
| DOT | MIX | UP | UP | 35.2 | 68.6 | 24.5 | 43.0 | 1.97 | 11% | **−9.38** |
| LTC | **DOWN** | MIX | UP | 27.4 | 64.2 | 17.5 | 45.8 | 0.75 | 28% | −0.73 |
| UNI | **DOWN** | MIX | UP | 59.7 | 66.5 | 24.0 | 35.2 | 1.37 | 15% | −2.60 |

**Every one of the ten coins is red on the day** and the 1H layer has finished
the rollover that started 09-05: **6 DOWN, 4 MIXED, zero UP — the first
all-negative 1H board since the watchlist widened.** The 1D layer is still
uniformly UP (ADX 27.4–61.4) — nothing is structurally broken yet, this is one
hard down-day inside intact dailies — but daily RSI has cooled across the board
(DOT 82.3→68.6, UNI 73.2→66.5, LTC 69.7→64.2, LINK 66.5→58.2).

7 of 10 clear `MIN_ATR_PCT` 0.75%. BTC/ETH/AVAX/SOL/LTC drift around or below it
(44%/37%/32%/28%/28% of 1R in costs) — the filter working, deliberately *not*
in `blocked_symbols`.

## FINDING 1 — the binding filter today is the EMA stack, and the log cannot show it

`evaluate_entry` returns the **first** failing filter, and the ATR floor and the
ADX bar both run *before* the `stack(h1) != "UP"` check (`strategy.py:63-74`).
So the rejection log reads like a fee-and-ADX day —

```
reject BTC: fee drag 44% of 1R (ATR 0.48% < 0.75%)
reject DOGE: ADX 12.0 < 20
reject LTC: fee drag 28% of 1R (ATR 0.75% < 0.75%)
```

— while concealing that BTC is also 1H DOWN and DOGE 1H MIXED. **Nothing on the
board could have been bought today at any setting**, because no coin has a 1H UP
stack. Two consequences worth carrying forward:

1. **Today's three blocks were provably inert.** LINK (ATR 1.15%, ADX 36.6),
   DOT (1.97%, 24.5) and UNI (1.37%, 24.0) all clear both the volatility floor
   and the ADX-20 bar — they are exactly the names where a block *could* bite —
   and every one of them would have been rejected one step later on
   `1H stack DOWN/MIXED`. The block cost zero trades, and it also bought zero
   protection; the log line `blocked by policy` masked the real gate.
2. A second layer sits behind the stack: 1H RSI is now **below the
   `ENTRY_RSI_MIN` 45 floor on 5 of 10** (LINK 34.3, UNI 35.2, DOGE 41.5,
   AVAX 41.5, DOT 43.0). Even a clean stack repair overnight leaves those five
   rejected on RSI until momentum rebuilds.

Diagnostic rule: on a no-fill day the stated rejection reasons can be entirely
non-binding. Read the stack column separately before concluding what stopped
the board — otherwise you tune the filter that happened to fire first.

## FINDING 2 — both open positions are running with an already-expired throttle

The 09-08 defect (`REENTRY_THROTTLE_HOURS` keyed to entry time, not exit time —
`ledger.throttled()` reads `last_entry_attempt`, written only at entry,
`trader.py:141`) is now live on **both** open positions:

| pos | entered | throttle expires | now | state |
|-----|---------|------------------|-----|-------|
| AVAX | 09-08 04:14 | 09-09 04:14 | 16:14 | **expired 12h ago** |
| XRP | 09-08 18:14 | 09-09 18:14 | 16:14 | expires in ~2h |

And both are close to their stops after today's sell-off:

| pos | entry | now | vs entry | stop | distance | R-progress |
|-----|-------|-----|----------|------|----------|------------|
| AVAX | 141.196 | 138.598 | −1.84% | 137.660 | **0.68% = 1.03 ATR** | −0.73R |
| XRP | 25.149 | 24.893 | −1.02% | 24.569 | 1.32% = 1.65 ATR | −0.44R |

Neither ever reached +1R, so both still sit on their **initial** 3.0×ATR stop —
the 2.0×ATR trail has never engaged, and forcing `risk_off` to tighten it to
1.5× would therefore change nothing. AVAX is inside one ATR of its stop; a stop
tomorrow is the base case. When it fires, `throttled()` will not fire with it,
and the same-cycle re-buy from 09-08 repeats — that one cost **Rp1.804 (0.34%
of equity) for zero change in exposure**.

**The policy overlay is the only cooldown available**, and it fits cleanly:
`blocked_symbols` is checked *after* `already open` (`strategy.py:110-115`) and
never forces an exit (it only feeds `entry_candidates`, `trader.py:244`). So
pre-blocking an open position is **free while the position lives and becomes the
missing cooldown the instant it stops**. That is the whole justification for
blocking AVAX and XRP tonight; it reaches something the engine genuinely cannot
see — the staleness of its own throttle.

## FINDING 3 — cash-only equity trap, 4th time in five days

The routine's own command printed:

```
equity Rp262.163 | cash Rp262.163      <- WRONG, cash only
```

against a true marked **Rp519.614** (`logs/trader.log` 16:14, reproduced exactly
by `broker.get_account(price_by_symbol={sym: 1H last_close})`). A fake **−48%
drawdown**, the worst distortion yet, because for the first time *both* slots
are open so nearly half of equity is in coin. On a live-money bot this has now
cried wolf on 09-05, 09-06, 09-08 and 09-09. The fix is one argument in the
routine's step-4c command; `get_account` already supports it (`broker.py:104`).

## Doctrine and ledger

Zero exits today — the first no-exit day since 09-03 — so lifetime is unchanged:
**8 closed, 4 TP / 4 stop = true win rate 50%, stop rate 50%.** A naive `pnl > 0`
count would still report 5/8 = 62.5% (AVAX's positive trailing-stop exit on
09-08 is a FAILURE by the standing rule).

Ledger overstatement got worse, not better:

| source | figure |
|--------|--------|
| `trades.json` closed-P&L sum | **+Rp46.652** |
| true equity delta (Rp500.861 → Rp519.614) | **+Rp18.753 (+3.7%)** |
| overstatement | **~149%** (was ~100% on 09-08) |

Three known defects drive it (`ledger.py:63` gross-only/no fee term, the
`avg_fill_price` prefixed-id bug, the :14 execution lag) and today a fourth
contribution appeared: the ledger does not mark open positions, so today's
**−Rp3.735 unrealized** is invisible to it. `risk.circuit_breaker_tripped()`
sums that same optimistic field, so the breaker is measuring a number ~2.5x too
kind.

## Top setups

**None.** No coin has a 1H UP stack; there is no long setup on this board and
this bot is long-only. Watch-list for tomorrow, in order:

- **LINK** — watch. Best 1D structure on the board (ADX 61.4, still UP) and the
  cheapest fee profile of the liquid names (18% of 1R). But it is the second
  worst performer today (−4.49%) with 1H DOWN at ADX 36.6 — a *strong* hourly
  downtrend, i.e. high ADX pointing the wrong way. Only interesting once the 1H
  stack flips UP; its 1H RSI 34.3 must also climb back over 45.
- **DOT** — watch, blocked. Cheapest drag on the board (11%, ATR 1.97%) and the
  most likely name to be bought on a bounce, which is precisely the problem —
  see the block rationale below.
- **XRP / AVAX** — held, both underwater and inside ~1.0–1.7 ATR of their stops.

## Risks

1. AVAX 1.03 ATR from its stop with a dead throttle — highest-probability event
   tomorrow, and the one with a known Rp1.8k self-inflicted tail.
2. Synchronised sell-off: all 10 red, DOT −9.38% and LINK −4.49%. Correlation is
   ~1 today, so `max_positions` is not real diversification.
3. DOT unwinding a parabola (+11.74% on 09-08 → −9.38% today, 1D RSI 82.3→68.6).
   The 1H layer cannot see that the move it might buy is a dead-cat inside a
   reversal.
4. Ledger/breaker measuring a ~2.5x-overstated P&L (Finding 3 above).

## Policy written

```json
{"date": "2026-09-09", "regime_hint": "auto",
 "blocked_symbols": ["DOT", "AVAX", "XRP"], "max_positions": 3}
```

**`regime_hint: auto` — 9th straight day, traced again.** Computed regime is
`risk_on` off an intact BTC 1D (UP, ADX 50.9). A cautious hint would (a) raise
the 1H ADX floor 20→25 and (b) tighten the trail 2.0→1.5×ATR. Both are inert
today: the gate that binds is the stack filter *three steps before* ADX, and
neither open position has reached +1R so no trail exists to tighten. Worse, per
the 09-02 corollary the cautious hint is **anti-selective** — it preferentially
admits the extended names, which carry high ADX by construction (LINK 36.6,
DOT 24.5) while rejecting the calm ones. Nothing here is worth that trade.

**Blocks — released 2 of 3, added 2, each with a release criterion:**

- **UNI RELEASED.** Stated criterion (1D RSI < 70) met at 66.5 after 9 sessions
  above. A block must drop when its reason resolves (09-02 corollary).
- **LINK RELEASED.** Post-stop cooldown expired 13:14 UTC today as specified.
  Nothing engine-invisible remains; its 1H DOWN stack is something the engine
  reads perfectly well for itself.
- **DOT re-blocked on a NEW reason.** Its old criterion (1D RSI < 70) is met at
  68.6, so the daily-extension block lapses — but the blow-off is now
  *unwinding*, not resolved: −9.38% in one session, the worst on the board,
  reversing 09-08's +11.74%. DOT has the cheapest drag (11%) and the second
  highest 1H ADX, so it is the single most likely name to get bought on a bounce
  the 1H layer will read as a fresh trend. Release when the reversal completes:
  **one green day with the 1H stack UP and ADX ≥ 25, or 1D RSI < 60.**
- **AVAX and XRP NEW — pre-emptive post-stop cooldown** (Finding 2). Free while
  they are open, and the only cooldown that exists if they stop, since their
  entry-keyed throttles have already expired. Release: **drop at the next daily
  write if the position is still open; if it stopped, hold 24h from the exit.**
- Not blocked on purpose: BTC/ETH/SOL/LTC (44%/37%/28%/28% of 1R in costs).
  `MIN_ATR_PCT` rejects them structurally; duplicating it cost 180 rejections in
  week one.

**`max_positions: 3`** with 2 open = one free slot. Honest caution on a board
where every name is red, correlation is ~1, and both existing positions are
underwater — without freezing the book against a genuine reversal.
