# CryptoIndodaxBot — Analysis 2026-09-10 (UTC)

Snapshot: `data/snapshots/2026-09-10/16.json`, 17 hourly snapshots today.
Engine regime: **computed `risk_on`** (BTC 1D stack UP, ADX 49.3), policy hint
`auto` → effective `risk_on`. Account **flat** (0 positions, all cash).

## Headline

**Second consecutive all-red day, and the pullback has now propagated up a
timeframe.** All 10 coins closed red (−1.04% ETH to −3.55% UNI) and — for the
second straight session — **0 of 10 have a 1H UP stack**. Yesterday the damage
was confined to the 1H layer; today the 4H layer joined it: 4 DOWN
(BTC/SOL/XRP/DOGE), 5 MIXED (ETH/AVAX/LINK/LTC/UNI), only DOT still UP. The
daily layer is untouched — **all 10 remain 1D UP** at 1D ADX 27.2–59.7. So the
structure is still "a multi-day pullback inside intact dailies", but it is one
timeframe deeper than it was 24h ago.

## Counterfactual first (the 09-09 lesson, applied)

Yesterday's top finding was that the rejection log is unreliable for judging
blocks, because `entry_candidates` checks `blocked` **before** calling
`evaluate_entry` (strategy.py:110-115), so a blocked coin never prints its
technical reason — and `evaluate_entry` itself returns the FIRST failure. So I
re-ran the board with `blocked=()` before writing any policy:

```
BTC  fee drag 57% of 1R (ATR 0.37%)   SOL  1H stack DOWN
ETH  fee drag 37% of 1R (ATR 0.56%)   DOGE 1H stack DOWN
XRP  fee drag 29% of 1R (ATR 0.73%)   AVAX 1H stack DOWN
LTC  fee drag 30% of 1R (ATR 0.71%)   LINK 1H stack DOWN
                                      DOT  1H stack DOWN
                                      UNI  1H stack DOWN
PASS: []  ← zero candidates with NO blocks at all
```

**Every one of yesterday's three blocks (XRP, AVAX, DOT) was inert today** —
verified, not assumed. Nothing was buyable at any setting, for the second day
running. Note XRP's true reason is *not* the block and *not* the stack it would
have hit next: its 1H ATR decayed to 0.73%, just under the 0.75% floor, so it is
now fee-rejected in its own right.

A second, independent gate stands behind the stack: **1H RSI is below the
`ENTRY_RSI_MIN` 45 floor on all 10 coins** (highest is LINK 41.4). Two
unrelated filters would each have to clear before anything can fire.

## Per-coin

| Coin | Day % | 1H stack | 1H ADX | 1H RSI | ATR% | 4H | 1D RSI | Read |
|------|-------|----------|--------|--------|------|-----|--------|------|
| BTC  | −1.15 | DOWN | 29.8 | 33.3 | 0.37 | DOWN  | 54.2 | Fee-rejected (57% of 1R). Regime driver, 1D UP holds |
| ETH  | −1.04 | DOWN | 27.0 | 41.1 | 0.56 | MIXED | 57.6 | Fee-rejected (37%). Mildest decline on the board |
| SOL  | −1.72 | DOWN | 41.7 | 36.3 | 0.84 | DOWN  | 54.4 | Clears ATR floor; stack is the only thing missing |
| XRP  | −2.53 | DOWN | 47.8 | 29.4 | 0.73 | DOWN  | 52.3 | Just slipped under the ATR floor; deeply oversold |
| DOGE | −2.78 | DOWN | 56.6 | 27.7 | 0.87 | DOWN  | 49.1 | Most oversold 1H RSI + highest ADX = strongest downtrend |
| AVAX | −2.20 | DOWN | 29.1 | 38.5 | 0.76 | MIXED | 55.9 | Stopped out 09-09 21:14; barely clears ATR floor |
| LINK | −1.29 | DOWN | 49.8 | 41.4 | 1.02 | MIXED | 54.1 | Best RSI on the board, cheap drag — first to watch |
| DOT  | −2.91 | DOWN | 34.1 | 36.8 | 1.03 | **UP** | 63.0 | Only intact 4H. Blow-off still unwinding |
| LTC  | −1.51 | DOWN | 39.5 | 38.8 | 0.71 | MIXED | 55.2 | Fee-rejected (30%), ATR decayed under the floor |
| UNI  | −3.55 | DOWN | 55.4 | 40.2 | 1.67 | MIXED | 57.7 | Worst day on the board; cheapest drag (13%) |

No coin reported `error` or `no_data` — all 10 pairs returned clean data.

## Top setups

Long-only bot; **there is no valid long today** — that is the honest answer and
the engine reached it independently. Ranked as *watch* candidates for the
turn, on cost and proximity to the gates:

- **LINK — watch.** Highest 1H RSI (41.4) so it is closest to the 45 floor,
  ATR 1.02% pays its fees comfortably, 1D UP at the board's highest 1D ADX
  (59.7). Needs a stack flip; nothing else is far away.
- **UNI — watch.** Cheapest fee drag on the board (ATR 1.67%) and 1H ADX 55.4,
  but it just printed the worst day (−3.55%), so a flip here is a knife-catch
  until the 1H closes green.
- **SOL — watch.** ATR 0.84% clears the floor and the 4H is DOWN rather than
  MIXED; needs the most repair of the three.
- **BTC / ETH / LTC — not setups.** Fee-rejected structurally (57%/37%/30% of
  1R). Per the 09-07 engine change these are **not** hand-blocked;
  `MIN_ATR_PCT` is doing its job.

## Risk flags

- **Both open positions stopped out late yesterday, after the digest was
  written** — XRP 09-09 20:14 UTC (−Rp3.061) and AVAX 09-09 21:14 UTC
  (−Rp3.322), −Rp6.383 combined. Yesterday's policy called this exactly ("a
  stop tomorrow is the base case") and pre-blocked both as a synthetic cooldown;
  that call was correct and the block became the cooldown the moment they closed.
- **Four consecutive stop exits (09-08 → 09-09).** The last profitable exit was
  09-07. See the clustering note below.
- **The 4H layer rolling over** is the genuine new deterioration; a pullback
  that only owns the 1H is noise, one that owns the 4H on 9 of 10 names is a
  trend change candidate. The 1D layer is the line that has not broken.
- **DOT is the split signal** — the only 4H UP, but also the only 1D RSI still
  elevated (63.0) and the unwinding blow-off from 09-08 (+11.74%, then −9.38%,
  then −2.91%). Its intact 4H makes it the *most* likely name to flip 1H UP
  first, which is precisely the risk: it would be bought into an unwind.

## Accounting

**The equity read is clean today — first time in six days.** With 0 open
positions `cash == equity`, so the cash-only trap in step 4c cannot fire:
`Rp515.854` matches `logs/trader.log` ("account equity Rp515.854 (cash
Rp515.854), 0 coin positions") exactly. The `price_by_symbol=` fix is still
needed for any day with positions open.

**Ledger overstatement, measured clean for the first time.** A flat book means
there is no open-position marking confound left in the number:

- ledger lifetime P&L: **+Rp40.269**
- true: Rp500.861 → Rp515.854 = **+Rp14.993**
- pure overstatement: **Rp25.276 — the ledger reports 2.7x reality**

Yesterday the gap was Rp27.899 with 2 positions open; it shrank by Rp2.623 as
those two closed, which confirms the "ledger does not mark open positions"
contributor is a **timing artifact that self-corrects at exit**. What is left
today — Rp25.276 on a flat book — is the persistent fee/accounting defect with
nothing else mixed in. `risk.circuit_breaker_tripped()` still sums this same
2.7x-too-kind field, so the breaker is correspondingly hard to trip.

**Stop doctrine.** 10 closed trades: **4 tp / 6 stop = true win rate 40%, stop
rate 60%**, down from 50/50 yesterday. The naive `pnl > 0` count says **50%** —
the gap is AVAX 09-08, a stop that closed **+Rp3.052 green**. Under the standing
rule that is a failed trade regardless of the sign, and it is exactly the trade
that would inflate the headline.

## Trade clustering — the finding worth carrying

All four TP exits landed in a **two-day window (09-06 → 09-07)**; every exit
since has been a stop:

```
09-04 stop  stop
09-06 TP    TP
09-07 TP    TP
09-08 stop  stop
09-09 stop  stop
```

TP exits sum +Rp55.995, stop exits −Rp15.726. The entire true +Rp14.993 is one
rally window, and the bot has given back Rp15.726 across the six stops on either
side of it. This is the same shape CryptoAutoBot's replay harness flagged (long
beta that bleeds in chop, profit concentrated in one rally) and it is the
strongest argument yet for building a replay harness here before tuning
anything. **9 live days is not enough to claim an edge** — the honest statement
is that the sample is one rally plus two chop episodes.

## Policy decision

- `regime_hint`: **auto** (10th straight day), traced again. A cautious hint
  raises the 1H ADX floor 20→25 — **inert today, every coin is already ADX
  ≥27.0** — and tightens the trail 2.0→1.5xATR, also inert with zero positions
  open and no trail in existence. Per 09-02 it is additionally *anti-selective*:
  it admits the highest-ADX names first, which today are DOGE 56.6, UNI 55.4,
  LINK 49.8 — the three falling hardest. `risk_off` would add a genuinely
  binding "green on day" test, but with 0/10 passing two independent gates
  there is nothing for it to filter, and no macro event tomorrow to justify it.
- `blocked_symbols`: **["DOT"]**.
  - **XRP RELEASED, AVAX RELEASED.** Their criterion was "release at the next
    daily write if still open, else hold 24h from the exit". They are no longer
    open, and 24h from exit expires tonight at **20:14 / 21:14 UTC** — about 3h
    into this policy window. Blocks are window-granular, so holding them costs
    ~20h of eligibility to buy ~3h of protection. The 3h shortfall is
    unreachable in practice: both would need a DOWN→UP stack flip *and* RSI to
    climb from 29.4/38.5 to ≥45 inside three hours. Releasing, per the standing
    rule that a block drops when its reason resolves. Caveat recorded: the
    engine's own `ledger.throttled()` keys off `last_entry_attempt` (XRP
    09-08 18:14, AVAX 09-08 04:14 — both already expired), so after tonight
    there is **no engine-side cooldown** behind these two.
  - **DOT HELD.** Its pre-registered release was "one green day with 1H stack UP
    and ADX ≥25, **or** 1D RSI < 60". Today is red (−2.91%), the stack is DOWN,
    and 1D RSI is **63.0** — falling (68.6 → 63.0) but not through the line.
    Criterion not met, so the block stands rather than being talked down. It is
    also the one name the engine will misread first, being the only intact 4H.
    Same criterion carries forward unchanged.
  - Not blocked on purpose: BTC/ETH/LTC/XRP are fee-rejected structurally;
    duplicating `MIN_ATR_PCT` cost 180 rejections in week one.
- `max_positions`: **3** (not the default 4). The book is flat, so this only
  binds on the turn — and that is exactly when it matters: all 10 names are 1D
  UP and correlation in a snapback is ~1, so a single bounce hour could fill
  every slot with the same trade. 3 leaves the sizing honest on a Rp515.854
  account that has just taken four straight stops.
